import 'package:flutter/material.dart';

class StudentOutputView extends StatelessWidget {
  const StudentOutputView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
